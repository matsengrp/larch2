# Topology-landscape v1 golden fixture

The `topology_landscape_v1/` directory is a synthetic TI-0 byte-contract
fixture, not phylogenetic evidence. Its producer authority is
`phylo/tools/generate_topology_landscape_v1_fixture.py` in `tric-explore`.

The frozen SHA-256 of the bundle's exact `SHA256SUMS` bytes is:

```text
4f87e5bc2b2adb1ada1fb326152a912bae59d28de9ca0504fca0c9d63ff1016f
```

Changing that value requires an explicit schema/fixture review and synchronized
byte-parity checks in both repositories.
