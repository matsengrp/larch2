# WRIC chart-SPR search Phase-10 counter sanity table

Auto-generated by `doc/wric_phase10_sanity_table.sh`.  Diffs a
local-commit run's counters against the conservative Phase-0 baseline
on a fixture known to accept an improving move
(`test/wric_four_taxon_misplaced`), so the per-accept contract is
exercised for real (not vacuously) and a regression to "full rebuild
per accept" or "dense materialize per candidate" is visible.
Counter-only (no timing); see
`doc/WRIC-CHART-SPR-SEARCH-COUNTER-BASELINE.md` for the full baseline.


## Contracted counters

| counter | conservative (baseline) | local-commit (transient) | local-commit (cold) |
|---|---:|---:|---:|
| accepted_moves | 1 | 1 | 1 |
| local_commit_accepted_moves | 0 | 1 | 1 |
| sidecar_rebuilds_after_accept | 1 | 0 | 0 |
| overlay_materializations_for_accept_materialization | 1 | 0 | 0 |
| transient_chain_extensions_for_verification | 0 (no substrate) | 1 | 0 |
| overlay_materializations_for_exact_verification (cold path) | n/a | (see full report) | 1 |
| inside_rows_recomputed_on_commit | 0 | 3 | 3 |
| outside_rows_recomputed_on_commit | 0 | 7 | 7 |

## Contract

- The fixture accepts an improving move in every mode
  (`local_commit_accepted_moves > 0`), so the per-accept contract is
  non-vacuous (a regression that broke local commit could not hide
  behind an all-zero table).
- Conservative mode materializes and rebuilds per accept:
  `sidecar_rebuilds_after_accept > 0` and
  `overlay_materializations_for_accept_materialization > 0` -- the
  Phase-0 baseline reference.
- local-commit: `sidecar_rebuilds_after_accept == 0` and
  `overlay_materializations_for_accept_materialization == 0` (no per-accept
  dense materialization; the single allowed dense materialization is
  counted under `overlay_materializations_for_final_compaction`).
- local-commit reports the named `commit_mode` / `verification_mode` /
  `chain_per_accept_exactness_label` (see the full dagutil report).
- The umbrella `full_overlay_materializations` is nonzero on any exact
  run because per-candidate exact verification materializes; it only
  reaches 0 once Phase 8/9 eliminate that, which is why the per-accept
  counter above is the load-bearing one.

## Result: PASS

All contracted values hold.
